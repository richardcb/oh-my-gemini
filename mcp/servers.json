{
  "$schema": "https://geminicli.com/schemas/mcp-servers.json",
  "mcpServers": {
    "github": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-github"],
      "env": {
        "GITHUB_PERSONAL_ACCESS_TOKEN": "${GITHUB_PAT}"
      },
      "description": "GitHub repository access - read/write files, PRs, issues",
      "trust": true
    },
    "exa": {
      "httpUrl": "https://mcp.exa.ai/mcp",
      "description": "Exa web search - code examples, documentation, and research",
      "trust": true
    },
    "context7": {
      "command": "npx",
      "args": ["-y", "@context7/mcp-server"],
      "env": {
        "CONTEXT7_API_KEY": "${CONTEXT7_API_KEY}"
      },
      "description": "Context7 - library documentation lookup",
      "trust": true
    }
  },
  "_setup_instructions": {
    "github": {
      "required_for": ["@researcher file access", "PR creation", "issue management"],
      "steps": [
        "1. Go to https://github.com/settings/tokens",
        "2. Generate a new token (classic) with 'repo' scope",
        "3. Add to your environment: export GITHUB_PAT=your_token",
        "4. Or add to .gemini/settings.json mcpServers section"
      ]
    },
    "exa": {
      "required_for": ["@researcher web search", "code examples", "documentation lookup"],
      "steps": [
        "1. Works out of the box with free tier (rate limited)",
        "2. For higher limits: sign up at https://exa.ai",
        "3. Get API key from https://dashboard.exa.ai/api-keys",
        "4. Update httpUrl to: https://mcp.exa.ai/mcp?exaApiKey=YOUR_KEY"
      ]
    },
    "context7": {
      "required_for": ["Library documentation", "API reference lookup"],
      "steps": [
        "1. Sign up at https://context7.com",
        "2. Get your API key from settings",
        "3. Add to your environment: export CONTEXT7_API_KEY=your_key"
      ]
    }
  },
  "_usage_notes": {
    "agent_integration": "These MCP servers enhance agent capabilities:",
    "researcher": "Uses exa for web search, github for repo exploration",
    "architect": "Uses context7 for library docs when designing",
    "executor": "Uses github for reading existing code patterns"
  }
}
